/** The builder's own version, from package.json, and the level target, from tools/level-target/target.json. Both set in vite.config.ts. */
declare const __BUILDER_VERSION__: string
declare const __LEVEL_TARGET__: { fileLufsTarget: number; maxGain: number }
