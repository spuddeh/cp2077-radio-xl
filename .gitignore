# Claude Code (local only - never commit; these are internal notes)
CLAUDE.md

# Build output
plugin/build/

# Editor
.vscode/
.history/
*.log
images/

# Loopback recordings for the crackle investigation (#3); tens of MB, never committed
audio-tests/

# WolvenKit build output of the icon project under tools/; the shipped archive is DV's copy
tools/wolvenkit/
__pycache__/

# The example station package is built by tools/make-example-station.py; the zip is not committed
example-station/build/

# Release zips downloaded or built at the root are never committed
/*.zip

# level-target.py scratch: extracted banks, wwiser dumps and decoded vanilla audio, hundreds of MB
tools/level-target/work/
